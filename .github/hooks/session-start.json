{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "linux": "git config core.hooksPath .githooks && npm install && npx playwright install --with-deps chromium && npm run dev:emulators",
        "cwd": ".",
        "timeout": 600,
        "windows": ""
      }
    ]
  }
}
