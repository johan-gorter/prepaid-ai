{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "git config core.hooksPath .githooks && npm install && npx playwright install --with-deps chromium && npm run devservices",
        "cwd": ".",
        "timeout": 600
      }
    ]
  }
}
